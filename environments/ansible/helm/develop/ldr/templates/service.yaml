apiVersion: v1
kind: Service
metadata:
  name: {{ include "ldr-api.fullname" . }}
  labels:
    {{- include "ldr-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerport }}
      protocol: TCP
      name: http
  selector:
    {{- include "ldr-api.selectorLabels" . | nindent 4 }}

